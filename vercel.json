{
  "buildCommand": "uv pip install --system --break-system-packages -r requirements.txt",
  "installCommand": "",
  "outputDirectory": "./public",
  "framework": null,
  "functions": {
    "api/app.py": {
      "maxDuration": 60
    }
  }
}
