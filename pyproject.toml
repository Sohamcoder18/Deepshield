[project]
name = "deepshield"
version = "1.0.0"
description = "Deepfake detection system"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
